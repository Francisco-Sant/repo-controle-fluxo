name: Workflow Evento
on:
  push:
    branche: ["main", "feature/*"]
jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: Action de Teste
        run: echo "Action executada apenas em main e fatures"
  
